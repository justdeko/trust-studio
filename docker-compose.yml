version: "3.8"
services:
  backend:
    build: .
    command: python -u app.py
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    links:
      - db
  db:
    image: mongo
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
    - "3000:3000"
