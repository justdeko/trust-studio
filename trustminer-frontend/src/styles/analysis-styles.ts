import makeStyles from "@material-ui/core/styles/makeStyles";
import {createStyles, Theme} from "@material-ui/core";

export const useAnalysisStyles = makeStyles((theme: Theme) => createStyles({
    root: {
        flexGrow: 1,
    },
}))